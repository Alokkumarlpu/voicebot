import express from 'express';

const router = express.Router();

// Placeholder for decision service endpoints
router.post('/evaluate', (req, res) => {
  res.json({ message: 'Decision evaluation endpoint' });
});

export default router;
